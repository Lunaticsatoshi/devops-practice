{
    "version": "1.0.0",
    "services": {
        "api" : {
            "commands": ["npm run start", "yarn start"]
        },
        "web" : {
            "commands": ["npm run start", "yarn start"]
        }
    }
}